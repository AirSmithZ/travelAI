# 问题日志

> 记录开发过程中发现的问题、根因分析与修复状态。

---

## 问题总览

| # | 严重度 | 模块 | 问题摘要 | 状态 |
|---|--------|------|----------|------|
| P31 | 高 | 全链路 | parse 后无后续逻辑 | ✅ 已修复 |
| P34–P46 | — | 前端 | AI 聊天专项 | ✅ 已修复 |
| P49–P56, P33 | — | 后端 | chat/parse 专项 | ✅ 已修复 |
| **P61–P66** | **高/中** | **地理编码** | **autocomplete 搜索不可用** | **✅ 已修复** |
| **P67–P77** | **高/中** | **UX / 机酒 / 对话** | **2026-08-09 十一项体验债** | **✅ 已修复** |
| **P78–P79** | **高** | **地图 / 航班后端** | **节点连线被盖 / Ignav 城市码** | **✅ 已修复** |
| **P80** | **严重** | **生成 / 机酒锚定** | **生成行程忽略用户确认酒店** | **✅ 已修复** |
| **P81** | **高** | **地理编码 / 地图** | **节点坐标塌缩 + 连线不准** | **✅ 已修复** |
| **P82** | **高** | **地理编码** | **中文 POI 对英文源匹配差（动态双语）** | **✅ 已修复** |
| **P83** | **中** | **总览拖拽** | **跨区实线不跟随幽灵节点** | **✅ 已修复** |
| **P84** | **中** | **总览拖拽** | **松手后实线错位，滚动才对齐** | **✅ 已修复** |
| **P85** | **中** | **地图 / 天数** | **切换天圆点应跟当天** | **✅ 已修复** |
| **P86** | **高** | **地图** | **有节点计数但圆点全无（cluster 残留）** | **✅ 已修复** |
| **P87** | **高** | **地图 / 日期 UI** | **日期组件不统一 · 切天圆点错乱 · 悬空连线** | **✅ 已修复** |
| P60 | 低 | 产品 | 自动 generate vs R6 | ⏸ 忽略 |

---

## 开放 / 进行中

（无）

---

## 已关闭

### P87 · 日期 UI + 地图按天/总览分工 + 悬空连线（2026-08-09）✅

**产品分工**（对齐设计 §3.3.1）

| 模式 | 工具栏日期 | 地图 |
|------|------------|------|
| **单日** | `DayTabs`（截图1） | 仅当天节点 |
| **总览** | **不显示**（日期在矩阵列头） | **全部天** |

另：悬空连线 — 按天建边；单日不画易孤儿的 cross_day；可见时 `syncMapData`。

**验收**：总览无顶栏日期条；单日有 DayTabs 且切天换圆点；总览地图为全行程

---

### P86 · 地图有计数但圆点全无（2026-08-09）✅

**现象**：badge「第 1 天 · 5 个节点」但地图无圆点；仅一条蓝线悬空。

**根因**：P81 关掉 cluster 图层，但若 source 仍 `cluster: true` 且缺 cluster 层，circle 的 `!point_count` 过滤在城市缩放下会丢掉全部点；路线是独立 source 仍可画。

**修复**：按 style 检测 `cluster` 并强制重建非聚合 source；去掉 circle/label 的 `point_count` 过滤。

**验收**：硬刷新后当日圆点与线端点对齐；切 D1/D2 圆点集合变化

---

### P85 · 地图切换天圆点应跟当天（2026-08-09）✅

**现象**：切 Day 后地图圆点集合不变（用户确认指的是圆点，非左侧表单）。

**根因**：总览模式下地图曾画全部天，Chip 只改 `activeDayIndex`。

**修复**：地图按 `activeDayIndex` 过滤；地图视图下 DayChips 用 `selectDay`。

**验收**：切天 → badge 与圆点/路线同步为当天

---

### P84 · 松手后实线错位、滚动才对齐（2026-08-09）✅

**现象**：拖完节点后跨区实线端点有时对不准节点；一滚动总览又跳回正确位置。

**根因（交互一致性）**：`useOverviewNodeAnchors` 的 revision 原先只看「节点个数 / 天数 / 区域数」。松手写入 `overview_offset` 后 layout 坐标已变，但 revision 不变 → 不重新 `measure()`。`ResizeObserver` 只对尺寸变化敏感，**绝对定位的 left/top 移动不会触发**；滚动监听会 `measure()`，所以一滚就修好。期间 stale DOM 锚点仍覆盖正确的 `matrixPlacements`。

**修复**：`anchorRevision` 纳入各节点 `x/y` 指纹，拖拽提交后强制重测。

**验收**：跨区拖节点松手 → 实线立即贴边，无需滚动

---

### P83 · 总览拖拽跨区实线不同步（2026-08-09）✅

**现象**：拖节点时同格虚线跟手，跨区实线（如酒店↔餐厅）停在原点，交叉/卡顿感明显。

**根因**：跨区边走 `OverviewEdgeLayer`，OV-01 DOM 锚点量到的是原地 `ghost-source`，盖住了 `liveMatrixPlacements` 的幽灵坐标；同格边用 `edgeDragPreview` 增量，故虚线正常。

**修复**：拖拽中跳过活跃节点的 DOM 锚点覆盖；幽灵卡片不注册 `data-overview-node`。

**验收**：拖动跨区主链节点 → 实线端点跟幽灵（同格虚线行为不变）

---

### P82 · 动态双语地理编码（SerpApi + name_en + Wikidata）（2026-08-09）✅

**现象**：中文景点名走 Photon/Nominatim 时常命中城市中心或空结果；静态别名表覆盖面有限。

**方案（非硬编码主路径）**

1. **P0**：`GEOCODE_PROVIDERS=serpapi,photon,nominatim`（需 `SERPAPI_API_KEY`）
2. **P1**：生成节点输出 `name_en`；query 变体与名称评分**英文优先**
3. **P2**：主链失败时 Wikidata（P625）双语补查，仍受目的地围栏约束

**改动**：`itinerary_llm.py` · `geocoding.py` · `wikidata_geo.py` · FE `ItineraryNode.name_en`

**验收**：`python backend/scripts/test_geo09_variants.py` · `python backend/scripts/test_geocode_collapse.py` · 配置 SerpApi 后重新 generate

---

### P81 · 节点坐标重合与连线不准（2026-08-09）✅

**现象**：单日「5 个节点」地图只见 2 个点；连线指到空白或与圆点错位。

**根因**

1. **地理编码塌缩**：中文 POI 对英文命中名称分为 0 → 打分退化成「离目的地中心最近」→ 多个景点落到同一城市中心坐标（成功写入，不是失败回退）
2. **`node.update` 用 provider 标题覆盖节点名**，掩盖错配
3. **MapLibre clusterMaxZoom=14 + fit maxZoom=14**：`spreadMapNodes` 仅 ~60m，仍被聚合成 1–2 个气泡；线画在 feature 坐标上、眼看的是 cluster → 「连到空白」

**修复**

| 层 | 改动 |
|----|------|
| `geocoding.py` | 拒城市中心误命中；名称分&lt;1 不接受；payload 不改写 `name`；塌缩告警 |
| `spreadMapNodes.ts` | 120m 邻域成组、~220m 散开 |
| `TravelMap.tsx` | **关闭节点聚类**；fit maxZoom 16；连线仍用 display 坐标 |

**验收**：`python backend/scripts/test_geocode_collapse.py` · 重新生成并等地理编码完成后刷新地图

---

### P80 · 生成忽略用户确认酒店（2026-08-09）✅

**现象**：用户已确认/添加的酒店，生成后的行程晚宿是模型另造的酒店名；Stay 面板与路线图不一致。航班/片区有同类「只靠 prompt」风险。

**根因（非「没传 travel_intel」）**

1. HARD 文案只要求「靠近/体现片区」，未要求 **exact hotel name**
2. `_llm_to_itinerary` 原样接受模型编造的 hotel 节点，**无确定性合并**
3. 生成只替换 `itinerary`，`travel_intel.hotels.itinerary_node_id` 变 stale
4. 未确认的 proposed 片区曾被塞进 HARD，与用户酒店争抢

**修复**

| 层 | 改动 |
|----|------|
| Prompt | confirmed_hotels 必须同名；仅 confirmed zones 进 HARD；optimize 不得保留冲突酒店 |
| 后处理 | `intel_anchor_enforce.enforce_confirmed_hotels`：改名/注入/剔除编造晚宿 + `meta.hotel_bindings` |
| 前端 | `rebindHotelsAfterGenerate` 同步 `travel_intel.hotels` |

**验收**：`python backend/scripts/test_intel_anchor_enforce.py`

---

### P78–P79 · 地图节点连线 + 航班查无数据（2026-08-09）✅

| ID | 现象 | 根因 | 修复 |
|----|------|------|------|
| **P78** | 地图节点/连线看不见或错位 | 住宿片区 fill 插在 `NODE_CIRCLE` 之前，盖住 **cluster** 与路线；空 day 时旧路线不清理；连线用真坐标、点用 display 坐标 | 片区改插在 `NODE_CLUSTER` 下并 `moveLayer`；空 scope 清路线；连线改用 display 坐标 |
| **P79** | 航班 App 内 ranked 常为空（非纯前端） | `resolve_search_iata` 把北京/东京等收成 Trip.com **metro**（BJS/TYO），Ignav 只认机场 IATA；甚至 `PEK`→`BJS` | 新增 `resolve_ignav_iata` + `city_codes.json` 的 `ignav_iata`；deeplink 仍用 metro |

**验收**

```bash
python backend/scripts/test_flight_tripcom.py   # 含 ignav_iata 断言
# 北京→东京 应有 ranked（需 IGNAV_API_KEY + 后端重启）
curl -s localhost:8000/api/v1/flights/search -H 'Content-Type: application/json' \
  -d '{"origin":"北京","destination":"东京","date":"2026-10-16","include_ignav":true}'
```

**主键文件**：`TravelMap.tsx` · `stayZoneGeoJSON.ts` · `city_codes.py` · `city_codes.json` · `search.py`

---

### P67–P77 · UX / 机酒 / 对话十一项（2026-08-09）✅

**来源**：用户反馈 + 截图（住宿片区「酒店名称」默认填片区名）+ skill 分析（ui-ux-pro-max / design-taste / ai-chat-ui）。

| ID | 现象 | 根因 | 修复要点 | 主键文件 |
|----|------|------|----------|----------|
| **P67** | 对话框各板块提示词过时/术语化 | 空态仍写「确认后自动生成」；住宿 hint 混用 lodging/OTA | 按 phase 改空态/composer；住宿文案口语化 | `ChatPanel.tsx`, `StayZonePanel.tsx` |
| **P68** | 未生成行程仍可点「路线预览」展开空壳 | strip 无门禁；展开仅 EmptyPreview | 无行程且无片区 geometry 时禁用 strip | `InputPanel.tsx`, `PreviewPane.tsx` |
| **P69** | 右上角旅行弹出框样式异常 | portal 宽度/阴影/裁切与 token 不一致 | 对齐 radius/边框/z-index/overflow | `PlanSelector.css` |
| **P70** | 航班空结果仍提示「缓存命中」 | 空 `ranked` 也 `cacheSet` | 仅缓存有报价结果；空结果不标缓存 | `FlightIntelPanel.tsx`, `searchResultCache` 用法 |
| **P71** | 对话提取反复审批 | 规划期 destination/日期等强制 FormPatch | 无行程时 trip_request 低风险自动写+Undo；有行程后改字段需确认 | `lowRiskPatches.ts`, `ChatPanel.tsx` |
| **P72** | 酒店「确认添加」无反馈 | 成功无 toast；无行程时静默 return | toast + 对话 ack；无行程明确提示 | `StayZonePanel.tsx`, `usePlanStore.addHotelFromZone` |
| **P73** | 航班常查不到票却体验不清 | Ignav 未配/失败时仅「未找到」；与缓存文案打架 | 区分无源/无报价；突出 Trip 深链与手动添加 | `FlightIntelPanel.tsx` |
| **P74** | 「在地图查看并编辑」无反应 | map 折叠左栏；无行程不渲染 TravelMap | form/选中节点时不折叠；有片区时渲染地图 | `usePlanStore.selectLeftPanelCollapsed`, `PreviewPane.tsx`, `StayZonePanel.tsx` |
| **P75** | 「酒店名称」默认是片区名，步骤歧义 | `` `${zone.label} 附近酒店` `` 预填 | 两步：片区→搜酒店；默认空；展开自动检索 | `StayZonePanel.tsx` |
| **P76** | 玩法印证像「只有提示词」且不明显 | 入口仅生成后；摘要偏软 | 摘要强化；生成前可打开说明；strip 可见 | `PlanSummaryCard`/`planReadiness`, `InputPanel`, `EvidencePanel` |
| **P77** | 开销面板找不到 | 仅节点编辑有开销字段，无行程级入口 | 顶栏左侧「开销」按钮 → 抽屉汇总 | `ExpensePanel.tsx`, `AppShell.tsx` |

**验收**

- [x] 空航班结果不再显示「缓存命中」
- [x] 确认添加酒店有 toast / 失败有原因
- [x] 有片区 geometry 时可在地图查看；左栏不消失
- [x] 酒店名称默认空，列表选中后填入
- [x] 无行程时「路线预览」不可用（有片区地图除外）
- [x] 规划期 parse 自动写入 trip_request；生成后修改需确认
- [x] 顶栏可打开开销汇总
- [x] `npm run build` / 相关单测通过

**关联文档**：[TODO-布局与前端.md](./TODO-布局与前端.md) · [22-对话编排](./llm-travel-data/22-对话编排与玩法印证UX调研.md) · [开发进度.md](./开发进度.md)

---

### P61–P66 · 地理编码 autocomplete 专项（2026-06-29）✅

**原现象**：`GET /geocode/autocomplete?q=哥打基纳巴卢国际机场&city=亚庇` → `{"results":[]}`。

**根因**

1. 仅 Nominatim 单源，当前环境 **403 Forbidden**
2. Photon 未带 **User-Agent** 时同样 403（httpx 默认 UA 被拒）
3. 异常被 `return []` 吞掉，HTTP 200 误导前端
4. `city=亚庇` 无英文别名归一
5. 前端需手动点按钮，无 debounce 联想

**修复**

| # | 要点 | 文件 |
|---|------|------|
| P61 | Provider 链 `photon,nominatim` 回退；Photon/Nominatim 均带 `GEOCODE_USER_AGENT` | `geocode_providers.py`, `config.py` |
| P62 | 全源失败 → `502` + `GeocodeProviderError`；真无结果 → `warnings` | `geocode.py`, `geocode_providers.py` |
| P63 | `GeocodeProvider` 抽象 + `GEOCODE_PROVIDERS` / `PHOTON_BASE_URL` / `NOMINATIM_BASE_URL` | `geocode_providers.py`, `config.py` |
| P64 | `city_aliases.py`：`亚庇` → `Kota Kinabalu, Malaysia`；多 query 变体 | `city_aliases.py`, `geocoding.py` |
| P65 | `NodeCoordEditor` 输入 debounce 400ms 自动联想 + 候选列表 | `NodeCoordEditor.tsx` |
| P66 | `GeocodeApiError` 展示上游 502；空结果区分 `warnings` vs 服务失败 | `geocode.ts`, `NodeCoordEditor.tsx` |

**响应扩展**

```json
{
  "results": [...],
  "provider": "photon",
  "warnings": []
}
```

**验收**

```bash
# 后端实测（2026-06-29）
geocode_autocomplete('哥打基纳巴卢国际机场', '亚庇') → provider=photon, count≥1
npm run build ✅
```

**配置（可选 `.env`）**

```bash
GEOCODE_PROVIDERS=photon,nominatim
GEOCODE_USER_AGENT=TravelPlanner/0.2 (your-contact-url)
```

---

### 历史批次（摘要）

- **P31**：parse → generate 衔接 ✅
- **P34–P46**：AI 聊天前端 ✅
- **P49–P56, P33**：chat/parse 后端 ✅

---

## 已忽略

### P60 · 自动 generate 与设计 R6 冲突

维持现状：确认 trip_request patch 后自动触发 generate，不改为纯手动按钮流程。

---

*最后更新：2026-08-09 · P87 日期统一与地图按天连线*
