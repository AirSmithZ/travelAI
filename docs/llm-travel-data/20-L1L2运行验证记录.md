# L1 / L2 运行验证记录

← [17-L 层释义](./17-用户决策回应与Geocode机酒重排.md) · [复检报告](../复检报告-travelAI-drag_dev2.md) · [TODO](./TODO.md)

> **验证日期**：2026-08-07 · **分支**：`drag_dev2` @ `2cf2650`（验证时）  
> **方式**：启动 `uvicorn app.main:app --host 127.0.0.1 --port 8000`，对 `/api/v1` 发真实 HTTP 请求 + 对照单测。

---

## 1. 总评（验证当时）

| 层级 | 结论 | 说明 |
|------|------|------|
| **L1 地图可落地** | **未稳定** | 围栏机制有效，但 Photon/Nominatim SSL/超时导致关键 POI 404；中心解析失败时曾整体放开围栏（复检后已修降级路径） |
| **L2 机酒锚点** | **契约通过；样例行为通过** | 无航班 400；带 `arrive_at=16:30` 的真实 LLM generate Day1 未出现抵达前重行程。需更多样例才可宣称稳定 |

---

## 2. HTTP 实测明细

| # | 检查 | 结果 | 细节 |
|---|------|------|------|
| 1 | `GET /geocode/autocomplete?q=Singapore` | ❌ | **502**；Photon/Nominatim SSL `UNEXPECTED_EOF` |
| 2 | `POST /geocode/search` 滨海湾金沙@新加坡 | ❌ | **404**（上游失败，非错点到别国） |
| 3 | `POST /geocode/search` Central Park@新加坡 | ✅ | Jurong Central Park `1.3379,103.7077`（新加坡内） |
| 4 | `POST /itineraries/geocode-nodes` 3 POI | ✅ | 成功点均在 SG；失败未写出界坐标；warning：中心未解析 / 部分失败 |
| 5 | `POST /itineraries/generate` 无 flights | ✅ | **400** FLOW-01b |
| 6 | `POST /itineraries/generate` + PVG→SIN 16:30 抵达 | ✅ | `date0=2026-10-16`；节点：机场 16:30 → transit 17:15 → hotel 17:45 → 餐 18:30；`violate_before_arrive=False`；TikHub **402** → evidence=0 |

**汇总**：4/6 通过。

### L2 样例节点（摘要）

```text
樟宜机场 16:30 (airport)
前往滨海湾金沙酒店 17:15 (transit)
滨海湾金沙酒店 17:45 (hotel)
珍宝海鲜楼 18:30 (restaurant)
滨海湾花园超级树 20:00 (attraction)
```

DeepSeek generate `latency_ms≈77797`，HTTP 200。

---

## 3. 单测（同日）

```text
test_geocode_fence.py — PASS
test_itinerary_dates.py — PASS
test_flight_rank.py — PASS
test_tikhub_evidence.py — PASS（mock）
```

---

## 4. 复检后追加修复（同日后续）

见 [复检报告](../复检报告-travelAI-drag_dev2.md) 核验：

- **SEC-01**：根目录档案曾出库，但 `backend/.edreams_chrome_data` + `flight-spike/.edreams_chrome_data` 仍被跟踪（属实）→ 已 `git rm --cached` 两路径；gitignore 加强。
- **围栏中心失败放开裸名**（属实）→ 保留 countrycodes、禁止裸名全球 Top1、无围栏时 `coord_confidence=low`。
- **围栏内近但错** → `geocode_place` / apply 多候选 + 名称相关性。
- **信心降级不一致** → 统一口径。

历史 blob 若曾 push，仍需账号轮换 + 可选 filter-repo（运维项，非单次 commit 可消）。

---

*记录版本：v1.0 · 2026-08-07*
