# L1 / L2 运行验证记录

← [17-L 层释义](./17-用户决策回应与Geocode机酒重排.md) · [复检报告](../复检报告-travelAI-drag_dev2.md) · [TODO](./TODO.md)

> **验证日期**：2026-08-07 · **分支**：`drag_dev2` @ `2cf2650`（验证时）  
> **方式**：启动 `uvicorn app.main:app --host 127.0.0.1 --port 8000`，对 `/api/v1` 发真实 HTTP 请求 + 对照单测。

---

## 1. 总评（验证当时）

| 层级 | 结论 | 说明 |
|------|------|------|
| **L1 地图可落地** | **主路径已加固（SerpApi）**；早期验证未稳定 | §2 时 Photon/Nominatim SSL 导致关键 POI 失败；§5 起默认 SerpApi 主源，实况 POI 落点 OK；仍受月配额与上游可用性约束 |
| **L2 机酒锚点** | **契约通过；样例行为通过** | 无航班 400；带 `arrive_at=16:30` 的真实 LLM generate Day1 未出现抵达前重行程。需更多样例才可宣称稳定 |

---

## 2. HTTP 实测明细

| # | 检查 | 结果 | 细节 |
|---|------|------|------|
| 1 | `GET /geocode/autocomplete?q=Singapore` | ❌ | **502**；Photon/Nominatim SSL `UNEXPECTED_EOF` |
| 2 | `POST /geocode/search` 滨海湾金沙@新加坡 | ❌ | **404**（上游失败，非错点到别国） |
| 3 | `POST /geocode/search` Central Park@新加坡 | ✅ | Jurong Central Park `1.3379,103.7077`（新加坡内） |
| 4 | `POST /itineraries/geocode-nodes` 3 POI | ✅ | 成功点均在 SG；失败未写出界坐标；warning：中心未解析 / 部分失败 |
| 5 | `POST /itineraries/generate` 无 flights | ✅ | **400** FLOW-01b |
| 6 | `POST /itineraries/generate` + PVG→SIN 16:30 抵达 | ✅ | `date0=2026-10-16`；节点：机场 16:30 → transit 17:15 → hotel 17:45 → 餐 18:30；`violate_before_arrive=False`；TikHub **402** → evidence=0 |

**汇总**：4/6 通过。

### L2 样例节点（摘要）

```text
樟宜机场 16:30 (airport)
前往滨海湾金沙酒店 17:15 (transit)
滨海湾金沙酒店 17:45 (hotel)
珍宝海鲜楼 18:30 (restaurant)
滨海湾花园超级树 20:00 (attraction)
```

DeepSeek generate `latency_ms≈77797`，HTTP 200。

---

## 3. 单测（同日）

```text
test_geocode_fence.py — PASS
test_itinerary_dates.py — PASS
test_flight_rank.py — PASS
test_tikhub_evidence.py — PASS（mock）
```

---

## 4. 复检后追加修复（同日后续）

见 [复检报告](../复检报告-travelAI-drag_dev2.md) 核验：

- **SEC-01**：根目录档案曾出库，但 `backend/.edreams_chrome_data` + `flight-spike/.edreams_chrome_data` 仍被跟踪（属实）→ 已 `git rm --cached` 两路径；gitignore 加强。
- **围栏中心失败放开裸名**（属实）→ 保留 countrycodes、禁止裸名全球 Top1、无围栏时 `coord_confidence=low`。
- **围栏内近但错** → `geocode_place` / apply 多候选 + 名称相关性。
- **信心降级不一致** → 统一口径。

历史 blob 若曾 push，仍需账号轮换 + 可选 filter-repo（运维项，非单次 commit 可消）。

---

## 5. GEO-03 SerpApi 接入后（同日）

> **方案**：L1 主地理源改 **SerpApi `engine=google_maps`**，Photon/Nominatim 作回退。密钥仅本地 `.env`（`SERPAPI_API_KEY`），模板见根 `.env.example`。

| 项 | 结果 |
|----|------|
| 配置 | `GEOCODE_PROVIDERS=serpapi,photon,nominatim`；无 Key 自动跳过 serpapi |
| 单测 | `scripts/test_serpapi_geocode.py` PASS |
| 实况 smoke | `Marina Bay Sands` → provider=`serpapi`，`1.2838,103.8591`；中文「滨海湾金沙@新加坡」同点、落在 SG bbox |
| HTTP 语义 | `/geocode/search` 上游全挂时改为 **502**（不再误报 404） |

**对 L1 的含义**：此前 Photon/Nominatim SSL EOF 导致的「关键 POI 搜不到 / 404」可由 SerpApi 主路径绕开；围栏与 GEO-06/07/08 仍作用于 SerpApi 候选。**配额**：Free Plan 约 250 次/月，生产需关注用量。Key 若曾出现在对话中，建议在 [SerpApi 控制台](https://serpapi.com/manage-api-key) 轮换。

---

## 6. 范围 2 契约 + 迭代行程（同日后续）

| 项 | 状态 | 说明 |
|----|------|------|
| DATA-03～09 | ✅ | 单 Mock JSON；Pydantic 校验；LLM tags/scene_group；compact/FormPatch；region 别名；storage 键名说明；tips「未联网核实」 |
| SEC-04 / FLT-LETSFG | ✅ | https+主机白名单；LetsFG 默认关并文档固化 |
| B-P4-04 | ✅ | `meta.intel_snapshot` / `intel_fingerprint` / `flight_quote_ids` |
| FLOW-02 | ✅ | `mode=generate\|optimize\|regenerate`；`IntelDirtyBanner`；**不**自动重跑 |
| HOT-03 | ✅ | `hotel_budget_per_night` + Stay 面板输入 + Trip 深链 `highPrice` + prompt |
| 单测 | ✅ | `test_generate_modes.py` · `test_itinerary_dates.py` · `tsc --noEmit` |

### 设计偏离复核（对照 18 / 06 / 16）

| 检查项 | 结论 |
|--------|------|
| 机酒优先、无两轮草图玩法 | ✅ 仍单次 generate；optimize/regenerate 为迭代模式非草图轮 |
| 改机酒绝不自动 regenerate | ✅ 仅 banner 按钮；confirmFlight 不触发 generate |
| 深链仅 `<a target=_blank>` | ✅ 全站无 `window.open` |
| 评分 5:2:2:1 | ✅ `rank.py` balanced 权重未改 |
| UGC 不写票价 | ✅ UNTRUSTED_UGC + EvidencePanel 文案保留 |
| Mock 单源 | ✅ FE import `backend/app/data/mock_singapore.json`；日期 10-16→19 连续 |

**曾发现并修正的偏离**：

1. FE/BE dirty 若用不同哈希算法会永远 dirty → 改为比对 `meta.intel_snapshot` 结构化快照。  
2. optimize/regenerate 失败时勿静默灌 Mock（会掩盖机酒冲突）→ store 对非 generate 模式失败只 toast。  
3. Mock 缺 10-17 → 已按连续日重算。

*记录版本：v1.2 · 2026-08-07*
