# 开发进度文档

> 更新：2026-06-29 · Phase 3s/3t/3u 完成（PDF 导出 / 总览虚拟化 / update_edge patch）

---

## 当前进度总览

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 0/1 – Phase 3r | ✅ | 表单、坐标、连线、总览、tips、场景分组等 |
| **Phase 3s** | ✅ | 路线图 PNG + PDF 导出 |
| **Phase 3t** | ✅ | 总览 >5 天紧凑模式 + 列虚拟化 |
| **Phase 3u** | ✅ | 对话 `update_edge` patch |
| Phase 4 | ⏳ | 移动端 |

**本地运行**

```bash
cd backend && uvicorn app.main:app --reload --port 8000
cd frontend && npm run dev
```

---

## Phase 3s / 3t / 3u 实现摘要 ✅

### PDF 导出（Phase 3s）

| 项 | 实现 |
|----|------|
| 依赖 | `jspdf` + 现有 `html-to-image` |
| API | `exportElementToPdf`、`exportOverviewShell(..., format)` |
| UI | `ExportGraphButton` 拆为 PNG / PDF 双按钮 |
| 范围 | 单日路线图 + 总览模式均支持 |

### 总览 >5 天虚拟化（Phase 3t）

| 项 | 实现 |
|----|------|
| 紧凑 | `getOverviewMetrics`：>5 天启用窄列宽 + 小卡片 |
| 虚拟化 | `useOverviewColumnVirtualizer` 仅渲染视口列 |
| 锚点 | `computeOverviewAnchors` 布局计算（不依赖 DOM） |
| 导出 | `exportOverviewCapture` 导出时临时渲染全部列 |
| UI | 紧凑模式角标 + 图例提示 |

### update_edge patch（Phase 3u）

| 项 | 实现 |
|----|------|
| 类型 | `FormPatchAction.update_edge`、`PatchTarget.edge` |
| 应用 | `applyItineraryPatch` → `updateEdgeInItinerary` / 跨日边 |
| 后端 | `chat_parse` supplement 模式支持 `update_edge` |
| UI | `PatchConfirmPanel` 交通/耗时/标注编辑（ai-chat-ui 确认流） |

### AI 聊天前端修复（2026-06-29）

| 项 | 实现 |
|----|------|
| 等待反馈 | parse 时 assistant 占位 + 脉冲光标 |
| 错误处理 | 4xx/5xx 不降级 demo；503 提示配置 Key |
| health | 读取 `llm_configured` |
| 确认流 | PatchConfirmCard 摘要嵌入 Panel；stepIndex / tags 修复 |
| 其它 | 消息折叠、fetch 超时、generate loading、selection 请求体 |

**涉及文件**：`ChatPanel.tsx`、`chat.ts`、`PatchConfirmPanel.tsx`、`PatchConfirmCard.tsx`、`usePlanStore.ts`、`chatHelpers.ts`、`patchDedupe.ts`

### AI 聊天后端修复（2026-06-29）

| 项 | 实现 |
|----|------|
| async parse | `chat_json_async` + `async def chat_parse` |
| warnings | `dropped_patch_count` + `warnings[]` 返回前端 |
| old_value | 从 trip_request / itinerary 填充 patch diff |
| selection | `ChatParseSelectionIn` + 摘要优先选中天 |
| 健壮性 | mode 纠正、JSON 修复轮 422、分类 502 |

**涉及文件**：`chat_parse.py`、`chat.py`、`schemas/chat.py`、`llm_client.py`

### 地理编码 autocomplete 修复（2026-06-29 · P61–P66）

| 项 | 实现 |
|----|------|
| 多源 | Photon + Nominatim 链式回退，`GEOCODE_PROVIDERS` 可配置 |
| 错误 | 全源失败 502；无匹配返回 `warnings` |
| 别名 | `city_aliases.py`（亚庇→Kota Kinabalu, Malaysia 等） |
| 前端 | debounce 联想、`GeocodeApiError`、provider 提示 |

**涉及文件**：`geocode_providers.py`、`geocoding.py`、`city_aliases.py`、`NodeCoordEditor.tsx`、`geocode.ts`

**涉及文件**：`exportGraphImage.ts`、`ExportGraphButton.tsx`、`layoutOverview.ts`、`OverviewGraphView.tsx`、`useOverviewColumnVirtualizer.ts`、`applyItineraryPatch.ts`、`PatchConfirmPanel.tsx`、`chat_parse.py`

---

## Phase 3q / 3r 实现摘要 ✅

场景虚线分组（P26）、`leftPanelMode` 持久化。

---

## 问题点（开放）

详见 [问题日志.md](./问题日志.md)。

| # | 问题 | 说明 |
|---|------|------|
| P31 | chat/parse 后无后续逻辑 | ✅ 已修复 |
| P34–P46 | AI 聊天前端专项 | ✅ 已修复 |
| P33/P49–P56 | AI 聊天后端 parse 专项 | ✅ 已修复（2026-06-29） |
| **P61–P66** | **地理编码 autocomplete** | **✅ 已修复（2026-06-29）** |
| P60 | 自动 generate vs R6 | ⏸ 忽略（维持自动 generate） |
| Phase 4 | 移动端 | 待排期 |

---

## TodoList（待完成事项）

### Phase 4 — 移动端

- [ ] 响应式 + 触摸手势

---

## 验收记录

- [x] Phase 3m–3r：连线、总览、tips、地图选点、场景分组、侧栏持久化
- [x] Phase 3s：PNG + PDF 导出（单日/总览）
- [x] Phase 3t：>5 天紧凑列宽 + 列虚拟化 + 导出全列
- [x] Phase 3u：对话 update_edge patch 前后端贯通
- [x] `npm run build` 通过（2026-06-29）

---

## 下一步建议

1. **Phase 4**：移动端响应式 + 触摸手势
