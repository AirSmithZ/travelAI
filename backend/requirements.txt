# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
pymysql==1.1.0
sqlalchemy==2.0.23
alembic==1.12.1

# Data Validation
# 使用稍新的 pydantic，让其自动选择当前环境可用的 pydantic-core 版本（避免编译老版本）
pydantic>=2.9.0,<3.0.0
pydantic-settings==2.1.0

# AI & LLM
# 使用兼容的 langchain 版本组合，让 pip 自动解决 langchain-core 依赖
langchain>=0.1.7
langchain-openai>=0.0.5
# 不固定 langchain-core，由依赖自动解决（langchain-openai 需要 >=0.1.16）
openai>=1.3.7,<2.0.0

# Async Task Queue
celery==5.3.4
redis==5.0.1

# HTTP Client
httpx==0.25.2
requests==2.31.0

# Utilities
python-dateutil==2.8.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Environment
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
